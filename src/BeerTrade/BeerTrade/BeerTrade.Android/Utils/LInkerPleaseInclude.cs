using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;
using DryIoc;

namespace BeerTrade.Droid.Utils
{
    public class LinkerPleaseInclude
    {
        public void PleaseIncludeDryIoc()
        {
            //var a = (IResolver)new object();
            //a.ResolveMany(null);
            //a.ResolveMany(null, default, null, null);
            //a.ResolveMany(null)
        }
    }
}